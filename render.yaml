services:
  - type: web
    name: global-history-lens
    runtime: node
    plan: free
    buildCommand: corepack enable && corepack prepare pnpm@10.4.1 --activate && pnpm install --frozen-lockfile --prod=false && pnpm build
    startCommand: NODE_ENV=production node dist/index.js
